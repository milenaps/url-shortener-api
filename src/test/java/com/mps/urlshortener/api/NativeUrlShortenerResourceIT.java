package com.mps.urlshortener.api;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeUrlShortenerResourceIT extends UrlShortenerResourceTest {

    // Execute the same tests but in native mode.
}